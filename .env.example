# Copy this file to .env and fill in values for local development.
# .env is gitignored and must never be committed.
# In production, set these via your platform's environment variable config.

# Port the server listens on
PORT=3000

# Controls logging format and error message verbosity
# development: dev-format morgan logs, full error messages
# production:  combined-format morgan logs, masked 5xx error messages
# test:        logging suppressed (set automatically by Jest)
NODE_ENV=development
